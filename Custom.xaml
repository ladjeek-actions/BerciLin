<local:MyCard Title="HajeekndのPCL" Margin="0,0,0,15" CanSwap="True" IsSwaped="False">
    <StackPanel Margin="25,40,23,15">
        <local:MyHint Text="这是Hajeekn高度折腾的效果" />
        <local:MyHint Margin="0,8,0,2" IsWarn="False"
                    Text="下一个卡片是快捷跳转的说" />
    </StackPanel>
</local:MyCard>

<local:MyCard Title="快捷跳转" Margin="0,0,0,15">
    <StackPanel Margin="25,40,23,15">
        <local:MyHint Margin="0,8,0,2" IsWarn="False"
                    Text="快捷跳转目录" />
        <local:MyButton Margin="0,8,0,10" Width="250" Height="35" ColorType="Highlight"
                Text="打开 Minecraft Wiki(维基百科)" EventType="打开网页" EventData="https://minecraft-zh.gamepedia.com/Minecraft_Wiki" ToolTip="点击打开 Minecraft Wiki！" />
        <local:MyButton Margin="0,8,0,10" Width="250" Height="35" ColorType="Highlight"
                Text="打开 Minecraft Wiki(哔哩哔哩游戏百科)" EventType="打开网页" EventData="https://wiki.biligame.com/mc/Minecraft_Wiki" />
        <local:MyButton Margin="0,8,0,10" Width="250" Height="35" ColorType="Highlight"
                Text="打开 MCBBS" EventType="打开网页" EventData="https://mcbbs.net/" />
        <local:MyButton Margin="0,8,0,10" Width="250" Height="35" ColorType="Highlight"
                Text="打开 Curseforge" EventType="打开网页" EventData="https://curseforge.com/" ToolTip="去畅游在模组的海洋中吧~" />
        <local:MyButton Margin="0,8,10,10" Width="140" Height="35" HorizontalAlignment="Left" Padding="13,0,13,0" ColorType="Red"
                Text="Hypixel" EventType="启动游戏" EventData="1.8.9|mc.hypixel.net" ToolTip="使用1.8.9进入全球最大的小游戏服务器 - Hypixel吧" />
                    </StackPanel>
</local:MyCard>

<local:MyCard Title="Hajeekndの说说" Margin="0,0,0,15" CanSwap="True" IsSwaped="False">
    <StackPanel Margin="25,40,23,15">
        <local:MyHint Margin="0,8,0,2" IsWarn="False"
                    Text="针不戳,有了个onepan.slqwq.cn看OneDrive网盘了" />
        <TextBlock FontSize="3px" Text="2021/5/4 13:31:06" HorizontalAlignment="Right" Margin="0,0,0,2" />
    </StackPanel>
</local:MyCard>

<!--

全部通用的属性：
 - Width、Height：设置宽度与高度
 - ToolTip：设置鼠标指向时的提示文本
 - Foreground：调整前景颜色（十六进制字符串，例如 #FF010203 代表 ARGB 中的 255,1,2,3。前两位代表不透明度，FF 为全不透明，00 为全透明）
 - Background：调整背景颜色（使用类似 Background="{DynamicResource ColorBrush5}" 的格式以调用 PCL2 当前的主题颜色，修改末尾的数字编号以改变颜色浓度）
 - Margin：调整外边距（格式为 左,上,右,下。例如 Margin="0,0,0,2" 即为 2 单位的下边距）
 - HorizontalAlignment：设置对齐方式（Left、Center、Right）

TextBlock（纯文本）：
 - Text：设置显示的文本（如果要使用引号等特殊字符，自行百度 XAML 转义）
 - TextWrapping：开启自动换行
 - FontSize：设置字号
 
Image（图片）：
 - Source：要显示的图片的网址

local:MyHint（提示条）：
 - Text：设置显示的文本（&#xA; 代表换行）
 - IsWarn：设置为 True 代表警告样式，False 代表提示样式
 
local:MyButton（按钮）：
 - Text：设置显示的文本
 - Padding：设置内边距，格式与 Margin 一致
 - ColorType：设置颜色主题（Highlight 为当前启动器的主题颜色，Red 为红色，默认为黑色）
 - EventType、EventData：触发特定事件
 
local:MyTextButton（文本按钮）：
 - Text：设置显示的文本
 - EventType、EventData：触发特定事件
 
local:MyCard（卡片）：
 - Title：设置显示的标题文本
 - CanSwap：这张卡片是否可以折叠，True 为是，False 为否
 - IsSwaped：这张卡片是否默认折叠，要求 CanSwap 必须为 True

-->
